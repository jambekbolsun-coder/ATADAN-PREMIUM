import { BarChart3, FileImage, Globe2, LayoutDashboard, LogOut, Tractor, X, Menu } from 'lucide-react'
import { NavLink, Outlet, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { supabase } from '../lib/supabase'
import { Wordmark } from './Wordmark'
const links=[['/admin',LayoutDashboard,'Обзор'],['/admin/tractors',Tractor,'Тракторы'],['/admin/content',Globe2,'Сайт'],['/admin/analytics',BarChart3,'Аналитика'],['/admin/media',FileImage,'Медиа']] as const
export function AdminLayout(){const[open,setOpen]=useState(false);const nav=useNavigate();const signOut=async()=>{await supabase.auth.signOut();nav('/admin/login')};const sidebar=<aside className="flex h-full w-[272px] flex-col bg-[#101510] p-5 text-white"><div className="px-2 py-3"><Wordmark inverse/></div><nav className="mt-8 space-y-1">{links.map(([to,Icon,label])=><NavLink key={to} end={to==='/admin'} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex min-h-12 items-center gap-3 rounded-2xl px-4 text-sm font-semibold transition ${isActive?'bg-atadan-500 text-[#102006]':'text-white/65 hover:bg-white/7 hover:text-white'}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="mt-auto pt-5"><button onClick={signOut} className="flex min-h-12 w-full items-center gap-3 rounded-2xl px-4 text-sm font-semibold text-white/60 hover:bg-white/7 hover:text-white"><LogOut size={18}/>Выйти</button></div></aside>;return <div className="min-h-screen bg-[#f5f7f3]"><div className="fixed inset-y-0 left-0 hidden lg:block">{sidebar}</div><div className="lg:pl-[272px]"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-black/5 bg-white/90 px-4 backdrop-blur lg:px-8"><button onClick={()=>setOpen(true)} className="grid h-11 w-11 place-items-center rounded-2xl bg-neutral-100 lg:hidden"><Menu/></button><div className="text-sm font-bold">ATADAN Admin</div><a href="/ru" target="_blank" className="rounded-full bg-neutral-100 px-4 py-2 text-xs font-bold">Открыть сайт</a></header><main className="p-4 sm:p-6 lg:p-8"><Outlet/></main></div>{open&&<div className="fixed inset-0 z-50 lg:hidden"><button className="absolute inset-0 bg-black/45" onClick={()=>setOpen(false)} aria-label="Закрыть"/><div className="relative h-full w-[272px]">{sidebar}<button onClick={()=>setOpen(false)} className="absolute right-3 top-3 grid h-10 w-10 place-items-center rounded-full bg-white/10"><X/></button></div></div>}</div>}
