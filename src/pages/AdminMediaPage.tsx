import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
import { MediaUploader } from '../components/MediaUploader'
export function AdminMediaPage(){const[files,setFiles]=useState<any[]>([]);const[loading,setLoading]=useState(true);const load=async()=>{const{data}=await supabase.storage.from('atadan-media').list('',{limit:100});setFiles(data||[]);setLoading(false)};useEffect(()=>{load()},[]);return <div><div><p className="text-xs font-bold uppercase tracking-[.18em] text-atadan-700">Storage</p><h1 className="mt-2 text-3xl font-extrabold tracking-[-.04em]">Медиа</h1><p className="mt-2 max-w-2xl text-sm text-neutral-500">Для контента сайта удобнее загружать изображения прямо в нужном разделе. Этот экран — общий доступ к Storage.</p></div><div className="mt-7 max-w-xl"><MediaUploader folder="library" onUploaded={()=>load()}/></div><div className="mt-6 rounded-[28px] bg-white p-5"><p className="text-sm text-neutral-500">Файлы хранятся в папках. В редакторе тракторов они привязываются к конкретной модели автоматически.</p><div className="mt-4 text-xs text-neutral-400">{loading?'Загрузка…':`Корневых элементов: ${files.length}`}</div></div></div>}
