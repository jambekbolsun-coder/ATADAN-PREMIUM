"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";

export default function ResetAdminPassword(){const [message,setMessage]=useState(""),[done,setDone]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setMessage("");const form=new FormData(event.currentTarget),password=String(form.get("password")??""),repeat=String(form.get("repeat")??"");if(password!==repeat){setMessage("Пароли не совпадают");return}const token=new URLSearchParams(window.location.search).get("token")??"",response=await fetch("/api/admin/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password})}),body=await response.json() as {error?:string};if(!response.ok){setMessage(body.error??"Не удалось восстановить доступ");return}setDone(true);setMessage("Пароль обновлён. Все старые сессии завершены.")}return <main className="admin-login-shell"><section className="admin-login-card"><span>ATADAN · безопасность</span><h1>Новый пароль</h1>{done?<><p>{message}</p><Link href="/admin">Перейти ко входу</Link></>:<form onSubmit={submit}><label><span>Новый пароль</span><input name="password" type="password" minLength={12} maxLength={128} required autoComplete="new-password"/></label><label><span>Повторите пароль</span><input name="repeat" type="password" minLength={12} maxLength={128} required autoComplete="new-password"/></label><small>Используйте не менее 12 символов.</small>{message?<p role="alert">{message}</p>:null}<button className="admin-primary" type="submit">Обновить пароль</button></form>}</section></main>}
